apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  labels:
    app: backend-pod
spec:
  selector:
    matchLabels:
      project: backend-pod
  template:
    metadata:
      labels:
        project: backend-pod
    spec:
      containers:
        - name: backend-pod
          image: paraslov/backend-for-devops:latest
          env:
            - name: PORT
              value: "8000"
          ports:
            - containerPort: 8080
  